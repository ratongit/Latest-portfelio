import Particles from "react-tsparticles"; 
import particalConfig from './particalConfig';
const ParticalBackgeound = () => {
    return (
        <Particles params={particalConfig}>

        </Particles>
    );
};

export default ParticalBackgeound;